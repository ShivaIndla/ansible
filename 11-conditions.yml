---
- name: conditions
  hosts:  app
  become: true
  tasks:
    - name: user exists or not
      ansible.builtin.debug:
        msg: "hellow"

    - name: process continuied
      ansible.builtin.debug:
       msg: "process continuied"

